// Exercícios de interpretação de código
// Questão 1

// A
// O código faz a divisão do número escolhido pelo o usuário por 2. Ele realiza o teste para saber se o número é par
// ou não.

// B
// Todos o números pares, ja que todo número par dividido por 2, o resultado da diferença vai ser igual a 0.

// C
// Todos o números que não forem pares, ja que todo número não par dividido por 2, o resultado da diferença vai ser
// diferente de 0.


// Questão 2

// A
// O código serve para mostrar ao usuário o preço da fruta que ele digitou (desde que ele digite corretamente, igual está
// no código).

// B
// "O preço da fruta  Maçã  é  R$  2.25", já que "Maça" consta no c´digo e foi escrita de forma "correta".

// C
// "O preço da fruta  Pêra  é  R$  5", já que o "break" serve para parar o código naquela linha.


// Questão 3

// A
// A primeira linha pede ao usuário que ele digite o primeiro número.

// B
// Se o usuário digitar "10" vai ser impresso, "esse número passou no teste", seguinda de um erro.
// Se o usuário digitar "-10", vai dar erro. Já que o código não foi declarado corretamente.

// C
// Sim, já que a variável mensagem foi declarada corretamente.




// Exercícios de escrita de código
const array = [1, 2, 3, 4, 5]
let novaLista = []
let i = 0
while ( i < Array.length) {
    let novoItem ="O elemento " + i.toString() + "é"
    novaLista.push(novoItem)
    i++
}

for (let i = 0; i < Array.length; i++ ) {
    if (Array.indexOf( arry[ i ]) === i) {
        models.push(array[ i ]);
    }
}
